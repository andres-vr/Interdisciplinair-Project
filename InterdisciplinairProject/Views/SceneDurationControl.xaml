<UserControl x:Class="InterdisciplinairProject.Views.SceneDurationControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="30" d:DesignWidth="50">
    
    <Border Background="White" 
            BorderBrush="#E0E0E0" 
            BorderThickness="1" 
            CornerRadius="10"
            Height="24"
            Width="46">
        <Border.Effect>
            <DropShadowEffect Color="Black" Opacity="0.1" BlurRadius="4" ShadowDepth="1" Direction="270"/>
        </Border.Effect>
        <TextBox Text="{Binding TotalDuration, Mode=TwoWay, UpdateSourceTrigger=LostFocus}"
                 HorizontalAlignment="Stretch"
                 VerticalAlignment="Center"
                 HorizontalContentAlignment="Center"
                 VerticalContentAlignment="Center"
                 FontSize="11"
                 FontWeight="SemiBold"
                 Foreground="#455A64"
                 Background="Transparent"
                 BorderThickness="0"
                 Padding="0"
                 ToolTip="Total Duration (ms) - minimum: FadeIn + FadeOut"/>
    </Border>
</UserControl>
